spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_react_template
    username: yaphet
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
      show-sql: true
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
app:
  spring-boot-template.base-url: http://localhost:8080